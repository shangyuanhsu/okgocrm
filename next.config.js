const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // env: {
  // apiKey: 'api-key',//process.env.apiKey
  // },
  images: {
    domains: ['picsum.photos']
  }
};

module.exports = nextConfig;
